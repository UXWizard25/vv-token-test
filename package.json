{
  "name": "@bild/design-tokens",
  "version": "1.0.0",
  "description": "BILD Design System Token Pipeline",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "prebuild": "node ./scripts/parse-figma-variables.mjs",
    "build": "npm run prebuild && style-dictionary build --config style-dictionary.config.cjs",
    "clean": "rimraf build src/processed-tokens",
    "postbuild": "node -e \"console.log('✅ Build finished')\""
  },
  "devDependencies": {
    "@tokens-studio/sd-transforms": "^1.3.0",
    "rimraf": "^6.0.1",
    "style-dictionary": "^4.3.0"
  }
}
